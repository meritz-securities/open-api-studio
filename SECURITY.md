# 보안 정책

메리츠증권 Open API 저장소의 취약점 신고 및 App Key 관리 정책입니다.

## 취약점 신고

인증 우회, 자격증명 노출, 권한 상승과 관련된 취약점을 발견한 경우 공개 이슈를 등록하지 말고 개발자 포털의 문의 채널로 신고하십시오.

- 개발자 포털: https://openapi.imeritz.com

신고 시 재현 절차와 예상 영향 범위를 함께 제공해 주시기 바랍니다.

## 앱키(App Key / Secret) 관리

- App Key와 App Secret은 인증 자격증명으로 취급하십시오. 코드, 저장소, 로그, 공유 채널에 기록하지 마십시오.
- 환경변수(`MERITZ_APP_KEY`, `MERITZ_APP_SECRET`)로 관리하십시오.
- 셸에서 `MERITZ_APP_KEY=... 명령` 형태로 앞에 붙여 실행하시면 셸 기록 파일에 값이 남습니다.
  기록이 남지 않는 방법으로 넣으십시오.
- 노출이 의심되는 경우 개발자 포털에서 즉시 재발급하십시오.

## 주문 요청 유의

기본값은 **운영 서버**(`openapi.imeritz.com:9443`)입니다. 다른 서버로 붙이려면
`MERITZ_BASE_URL` 과 `MERITZ_WS_URL` 을 둘 다 지정하십시오.

기본값은 `MERITZ_READ_ONLY=1`(조회 전용)입니다. 주문·환전을 쓰려면
`MERITZ_READ_ONLY=0` 으로 명시적으로 열어야 합니다.

주문·환전은 `--confirm` 에 미리보기 토큰을 넘겨야 전송됩니다. 이 절차는
`ApiClient.call()` 에 있어 `meritz` 명령과 파이썬 직접 호출 양쪽에 걸립니다.

미전송 주문의 지문과 만료 시각은 `~/.meritz/pending.json`(권한 600)에 남습니다.
공용 단말에서는 사용 후 삭제하십시오.
