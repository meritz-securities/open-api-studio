# 기여 안내

메리츠증권 Open API Studio 에 관심 가져 주셔서 감사합니다.
이 문서는 이슈를 올리시거나 코드를 보내실 때 참고하실 내용을 담았습니다.

## 먼저 확인해 주십시오

**계좌·주문·거래에 관한 문의는 이곳에서 처리해 드릴 수 없습니다.**

- 주문이 나가지 않았다, 잔고나 평가금액이 다르게 보인다, 체결 내역을 확인하고 싶다
- 앱키 발급, 계좌 연결, 이용 신청, 권한 부족
- API 명세·업무 규칙에 대한 문의

이런 내용은 **개발자 포털의 문의 채널**을 이용해 주십시오. 이 저장소는 명령줄
도구와 에이전트 스킬만 다루며, 저장소 관리자에게는 계좌를 조회하거나 주문 상태를
확인할 권한이 없습니다.

이곳에서 다루는 것은 **`meritz` 명령과 스킬**에 관한 문제입니다.

## 자격증명과 계좌 정보

이슈·PR·커밋에 다음을 절대 넣지 말아 주십시오.

- 앱키(App Key)·시크릿(App Secret)·접근토큰
- 계좌번호
- 조회 결과에 담긴 잔고·보유종목·주문번호 등 실제 값

**터미널 기록에는 `export MERITZ_APP_KEY=...` 가 그대로 남아 있습니다.** 명령과
출력을 옮겨 붙이실 때 특히 주의해 주십시오. 이슈는 누구나 볼 수 있고, 지우셔도
수정 이력에 남습니다. 이미 노출하셨다면 개발자 포털에서 **즉시 재발급**해 주십시오.

## 이슈

이슈 양식을 골라 주십시오. 빈 이슈는 열어 두지 않았습니다.

| 양식 | 언제 |
|---|---|
| meritz 명령이 동작하지 않습니다 | 설치·실행·호출에서 생긴 문제 |
| 명세·도움말이 실제와 다릅니다 | `meritz api show`·README·스킬 문서가 실제와 어긋날 때 |
| 이런 기능이 있었으면 합니다 | 새 명령 제안, 개선 제안 |

**실행하신 명령**과 **종료 코드**, 어느 서버였는지, `meritz --version`,
파이썬 버전을 적어 주시면 확인이 훨씬 빠릅니다. `meritz doctor` 결과를 함께
넣어 주시면 더 좋습니다.

## 개발 환경

Python 3.11 이상이 필요합니다.

```bash
git clone https://github.com/meritz-securities/open-api-studio.git
cd open-api-studio
pip install -e ".[realtime]" pytest
```

## 테스트

```bash
pytest tests/ -q            # 앱키·네트워크 없이 돕니다
pytest tests/ -q -m ""      # 설치 검증까지 (네트워크 필요)
```

기본 실행에서는 `slow` 표시가 붙은 설치 검증 테스트가 빠집니다. 그 테스트는
가상환경을 만들어 실제로 설치해 보므로 네트워크가 필요합니다.

**테스트를 위해 실제 API 를 호출하지 말아 주십시오.** 앱키에 연결된 계좌는
실계좌입니다.

## 코드를 보내실 때

이 도구는 실제 계좌에 주문을 낼 수 있습니다. 아래 약속은 안전 장치이므로 지켜 주십시오.

- **주문의 두 단계 확인을 우회하거나 약화시키지 말아 주십시오.** `--confirm` 없이
  실행하면 전송하지 않고 보낼 내용만 보여 줍니다. 토큰은 한 번만 쓸 수 있고 180초 뒤
  만료되며, 요청 값이나 대상 서버가 바뀌면 무효가 됩니다
- **종료 코드의 뜻을 바꾸지 말아 주십시오.** 스크립트가 결과를 판정하는 약속입니다
  (`0` 성공 / `2` 사용법 오류 / `3` 실패 / `4` 확인 필요 / `5` 주문 미접수)
- 주문 응답이 성공으로 와도 `warn_cls_code` 가 경고면 접수되지 않은 상태입니다.
  이 경우를 실패(종료 코드 `5`)로 처리하는 동작을 유지해 주십시오
- 사람이 읽는 출력과 `--json` 출력을 함께 지원하는 명령은 양쪽을 모두 고쳐 주십시오
- 코드 스타일은 `pyproject.toml` 의 ruff 설정을 따릅니다 (줄 길이 120)

### 스킬을 고치실 때

`skills/` 아래 문서는 카탈로그에서 자동으로 만들어지는 부분이 있습니다.
고치신 뒤에는 다시 만들어 주십시오.

```bash
python3 build_skills.py
```

## 기여하시는 방법

이 저장소는 사내 저장소에서 만들어 내보내는 **스냅숏**입니다. 배포할 때마다
전체를 새로 씁니다. 그래서 **여기에 올리신 PR 은 머지하지 않습니다** —
머지해도 다음 배포에서 지워지기 때문입니다.

대신 이렇게 처리합니다.

1. 포크해서 브랜치를 만들고 변경한 뒤 `pytest tests/ -q` 를 돌려 주십시오
2. PR 을 올려 주십시오. 내용과 의도를 읽는 데 그대로 씁니다
3. 채택하면 저희가 커밋을 그대로 가져갑니다(`cherry-pick`). 저작자는
   보내 주신 그대로 남습니다
4. 다음 배포에 담아 내보내고, PR 은 반영된 커밋을 적어 닫습니다

고치실 것을 글로 적는 편이 빠르시면 **이슈만 열어 주셔도 됩니다.** 코드가
없어도 됩니다. 재현 방법과 기대한 동작을 적어 주시면 그대로 다룹니다.

`main` 에 직접 푸시하실 수 없습니다. 배포 자동화만 씁니다.

## 보안 취약점

인증 우회·자격증명 노출·권한 상승, 확인 절차 우회와 관련된 내용은 **공개 이슈나
PR 로 올리지 마시고**, [SECURITY.md](SECURITY.md) 에 적힌 절차를 따라 주십시오.

## 라이선스와 권리

PR 을 보내시는 것은 아래에 동의하신다는 뜻입니다.

- 보내신 내용은 이 저장소와 같은 [MIT 라이선스](LICENSE)로 배포됩니다.
- 보내신 내용은 본인이 작성하셨거나 제출할 권한을 가지고 계십니다.
- 제3자의 코드·문서를 포함하셨다면 출처와 라이선스를 PR 에 적어 주십시오.
  MIT 와 함께 배포할 수 없는 라이선스의 코드는 받지 않습니다.
- 소속 기관이 저작권을 갖는 결과물이라면 제출 전에 그 기관의 허락을 받아 주십시오.

병합된 기여는 MIT 라이선스에 따라 메리츠증권과 다른 이용자가 이용합니다.
기여자의 저작권은 기여자에게 남습니다.
